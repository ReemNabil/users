import React from 'react';
import {Header,Text} from 'native-base';

export default ()=>{
    return <Header>
        <Text style={{fontSize:30,alignSelf:'center',color:'white'}}>UsersList</Text>
    </Header>
}